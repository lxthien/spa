<div class="col-lg-4 d-none hidden-xs hidden-sm" id="sidebar">
    <div class="row">
        <div class="col-md-12">
            <div class="sidebar">
                {{ render(controller(
                    'AppBundle:News:listNewsByCategory', { 'categoryId': 1, 'template':'news/prices.html.twig', 'title':'Dịch vụ làm đẹp tại Hải Beauty' }
                )) }}
                {{ include('widgets/hotNews.html.twig') }}
                {{ render(controller(
                    'AppBundle:News:listNewsByCategory', { 'categoryId': 1, 'template':'news/prices.html.twig', 'title':'Ưu đãi khuyến mãi' }
                )) }}
            </div>
        </div>
    </div>
</div>